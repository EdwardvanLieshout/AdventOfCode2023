console.log([...(require('fs').readFileSync)('./input03.input','utf-8').matchAll(/mul\(\d+\,\d+\)/g)].map((v)=>v[0]).map((v)=>v.substring(4).slice(0,-1).split(',')).map((n)=>+n[0]*+n[1]).reduce((p,a)=>p+a,0));